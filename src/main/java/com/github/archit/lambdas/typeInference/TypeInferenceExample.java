package com.github.archit.lambdas.typeInference;

public class TypeInferenceExample {
  /*
  public static void main(String[] args) {
    //defining a lambda expression of type TypeInterface
    //TypeInterface lambdaInterface = (String s) -> s.length();

    //Since the compiler can do type inference, we need not explicitly specify the
    //type of the parameter or the return type

    //now we can actually ignore this because of inline implementation
    //TypeInterface lambdaInterface = s -> s.length();
    //System.out.println(lambdaInterface.stringLength("Java"));

    //defining the implementation
    System.out.println(printLength(s -> s.length()));
  }

  //a method that takes in an object of type TypeInterface
  //similar to the greet method of greeter class
  public static int printLength(TypeInterface typeInterface) {
    String s = "Type Interface";
    //implementation to be defined by the caller
    return typeInterface.stringLength(s);
  }

   */
}
